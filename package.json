{
  "name": "clicklocal-mailer",
  "version": "1.0.0",
  "description": "Email sender for ClickLocal outreach campaigns",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run clean && node src/server.js",
    "dev": "node src/server.js",
    "test-send": "node src/test-send.js",
    "send": "node src/send-campaign.js",
    "status": "node src/check-status.js",
    "clean": "node -e \"require('fs').readdirSync('.').filter(f=>f.startsWith('tmpclaude-')||f==='nul').forEach(f=>require('fs').unlinkSync(f))\" 2>nul || exit 0"
  },
  "keywords": [],
  "author": "ClickLocal",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "googleapis": "^170.0.0",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.12",
    "uuid": "^13.0.0"
  }
}
